<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login</title>
    <style>
      @import url("https://fonts.googleapis.com/css2?family=Lato&display=swap");
      * {
        padding: 0;
        margin: 0;
        box-sizing: border-box;
        font-family: "Lato", sans-serif;
      }
      body {
        width: 100%;
        height: 100vh;
        background-color: #fcffe7;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
      }
      .titles {
        margin-bottom: 50px;
      }
      main {
        border-radius: 50px;
        width: 380px;
        height: 400px;
        background-color: #eb455f;
        display: flex;
        flex-direction: column;
        align-items: center;
      }
      main h3 {
        margin: 20px 0px;
      }

      form {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
      }
      input {
        padding: 10px 20px;
        background-color: #bad7e9;
        color: black;
        margin: 15px 0px;
      }
      .btn-login {
        padding: 15px 50px;
      }
    </style>
  </head>
  <body>
    <div class="titles">
      <h1>Fullstack APP</h1>
      <h2>Puedes acceder a tu sesion</h2>
    </div>

    <main>
      <h3>Ingresa tus datos...</h3>

      <% if (messages.success_msg) { %>
      <div><%= messages.success_msg %></div>
      <% } %>

      <!-- <h1>Login</h1> -->

      <!-- <% if (messages.error) { %>
      <div><%= messages.error %></div>
      <% } %> -->

      <form action="/users/login" method="post">
        <div>
          <input
            type="text"
            id="email"
            name="email"
            placeholder="Correo electronico"
            required
          />
        </div>
        <div>
          <input
            type="password"
            id="password"
            name="password"
            placeholder="Password"
            required
          />
        </div>

        <div>
          <input class="btn-login" type="submit" value="Login" />
        </div>
        <p>
          Aun no tienes una cuenta? <a href="/users/register">Haz click aqui</a>
        </p>
      </form>
    </main>
  </body>
</html>
